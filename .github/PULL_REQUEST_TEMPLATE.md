# Descrição

<!-- Descreva sucintamente a mudança e o motivo. -->

## Checklist técnico
- [ ] `npm run typecheck` passou
- [ ] `npm run lint` passou (se aplicável)
- [ ] `npm run db:generate` passou (se mudou schema)
- [ ] Migration SQL incluída em `supabase/supabase/migrations` ou `sql/` (se aplicável)
- [ ] Notas de deploy / rollback incluídas (se aplicável)
- [ ] Nenhum secret foi adicionado ao repositório

## Notas de deploy
<!-- Ex.: `psql "$DATABASE_URL" -f sql/001_...sql` ou `npm run sb:functions:deploy` -->

## Arquivos alterados relevantes
<!-- Liste arquivos principais modificados para revisão rápida -->

